/// <reference types="node" />
import { ServerHandler } from '../core/handler.js';
import { BaseEngine } from '../engines/base.js';
import http from 'node:http';
import type { RouteHandler, WebSocketHandler } from '../core/types.js';
import type { InitOptions, StartOptions } from '../engines/base.js';
import type { ViteDevServer } from 'vite';
export declare class NodeEngine extends BaseEngine {
    private _isRunning;
    private _router;
    private _server;
    private _vite;
    private _websocketHandlers;
    protected _serverHandler: ServerHandler | null;
    init(options: InitOptions): Promise<void>;
    getHttpServer(): http.Server<typeof http.IncomingMessage, typeof http.ServerResponse>;
    isRunning(): boolean;
    registerRoute(path: string, handler: RouteHandler): void;
    registerWebsocket(path: string, handler: WebSocketHandler): void;
    setupVite(vite: ViteDevServer): void;
    start(options: StartOptions): Promise<void>;
    stop(): Promise<void>;
}
