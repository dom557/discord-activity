import { BaseEngine } from './base.js';
import type { RouteHandler } from '../core/types.js';
import type { InitOptions, StartOptions } from './base.js';
import type { ViteDevServer } from 'vite';
export declare class FastifyEngine extends BaseEngine {
    private _isRunning;
    private _server;
    private _vite;
    init(options: InitOptions): Promise<void>;
    getHttpServer(): import("fastify").RawServerDefault;
    isRunning(): boolean;
    registerRoute(path: string, handler: RouteHandler): void;
    registerWebsocket(): void;
    setupVite(vite: ViteDevServer): void;
    start(options: StartOptions): Promise<void>;
    stop(): Promise<void>;
}
