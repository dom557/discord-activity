import type { BaseEngine } from '~/engines/base.js';
export declare let _readyPromiseResolve: () => void;
export declare function getServerEngine<T extends BaseEngine = BaseEngine>(): T;
/**
 * Returns a promise that resolves when the plugin is all set up.
 */
export declare function ready(): Promise<void>;
